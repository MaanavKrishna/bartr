# Copy to `.env.local` and adjust. Every value here is optional — the app boots
# with the defaults shown below.

# Absolute URL the app is served from. Used for metadata and canonical links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Product name shown in the UI.
NEXT_PUBLIC_APP_NAME=Bartr
